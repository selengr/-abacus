# Optional: Upstash Redis for durable public scores + multiplayer across serverless instances.
# Without these, the app uses in-memory + .data/store.json (fine for local / single Node server).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
